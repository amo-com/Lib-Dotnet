using Xunit;

namespace Amo.Lib.Test.Common
{
    public class CeProxyTest
    {
        /*
        [Fact]
        public void BuildTest()
        {
            var setting = CeProxy<ISiteSetting>.Build();
            Assert.NotNull(setting);
            setting = CeProxy<ISiteSetting>.Invoke(setting, Sites.HPN);
            Assert.True(setting.HasSite);
        }
        */
    }
}
